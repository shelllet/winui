# 发送输�?
通过网络，发送鼠标键盘事件�?

![SendInput](./images/11.png ':size=90%')

## 子流�?
> 不支�?

## 运行参数

* 主机
>  *IP* 必须为本�?IP*, 端口为有有效的端口，比如�?433，该参数是主机地址，如果是`UDP`协议�?IP*�?`0.0.0.0` 可以向局域网广播，鼠标键盘事件�?

* 偏移坐标
> 如果置为窗口，发送端会自动把全局坐标映射为窗口坐标。如果输入坐标， 发送端在发送坐标时加上该坐标的值�?

* 协议
> 支持 `UDP` �?`TCP` 协议，建议使�?`UDP` 协议，提高鼠标键盘数据的传输效率。注意：发送和接收端的协议必须相同�?

## 输出

> �?

### 其它

示例�?https://github.com/shelllet/WinUi/blob/main/network/send_receive_input.simple






