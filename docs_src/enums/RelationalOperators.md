# RelationalOperators

* GreaterThan
* EqualTo
* LessThan
* NotEqualTo
* GreaterThanOrEqualTo
* LessThanOrEqualTo

<!-- MDX break -->

| 操作�?| 意义       | 描述                                                                                          | 句法                   |
| ------ | ---------- | --------------------------------------------------------------------------------------------- | ---------------------- |
| ==     | 等于       | 如果第一个操作数等于第二个操作数，则等于运算符返�?`True`。否则，它返�?`False`�?            | `操作�?1 == 操作�?2` |
| !=     | 不等�?    | 如果第一个操作数不等于第二个操作数，则不等于运算符返�?`True`。否则，它返�?`False`           | `操作�?1 != 操作�?2` |
| >      | 大于       | 如果第一个操作数大于第二个操作数，则大于运算符返�?`True`。否则，它返�?`False`               | `操作�?1 > 操作�?2`  |
| `<`      | 小于       | 如果第一个操作数小于第二个，则小于运算符返回 `True`。否则，它返�?`False`�?            | `操作�?1 < 操作�?2`  |
| `>=`     | 大于或等�?| 如果第一个操作数大于或等于第二个操作数，则大于或等于运算符返�?`True`。否则，它返�?`False`�?| `操作�?1 >= 操作�?2` |
| `<=`     | 小于或等�?| 如果第一个操作数小于或等于第二个操作数，则小于或等于运算符返�?`True`。否则，它返�?`False`�?| `操作�?1 <= 操作�?2` |

